---
const attributes = ['Brand', 'Price', 'CPU Generation', 'RAM', 'Storage', 'Screen Size'];
---
<section class="rounded-xl bg-white p-4 shadow-sm">
  <h2 class="font-semibold">Filters</h2>
  <p class="mt-2 text-sm text-slate-600">Filters enhance with JavaScript when attributes exist; otherwise all products remain visible.</p>
  <div class="mt-3 flex flex-wrap gap-2">
    {attributes.map((attribute) => (
      <span class="rounded-full bg-slate-100 px-3 py-1 text-xs">{attribute}</span>
    ))}
  </div>
</section>
